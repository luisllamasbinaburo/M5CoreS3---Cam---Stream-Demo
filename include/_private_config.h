#ifndef __PRIVATE_CONFIG_H
#define __PRIVATE_CONFIG_H

// replace with your date, and remove _ in filename (so it ends in private_config.h)

const char* ssid = "ssid";
const char* password = "password";

#endif